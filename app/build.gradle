apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'

android {
    namespace 'com.abhi.minibrainage'
    compileSdk 34
    ndkVersion "26.1.10909125"

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlin {
        jvmToolchain(17)
    }

    defaultConfig {
        applicationId "com.abhi.minibrainage"
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 11
        versionName "1.2.1"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            // Deobfuscation file located under:
            // app/build/outputs/mapping/release/mapping.txt
            // AAB: BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'

            // Native debug symbols located under:
            // app/build/outputs/native-debug-symbols/variant-name/native-debug-symbols.zip
            // AAB: BUNDLE-METADATA/com.android.tools.build.debugsymbols
            // Or follow: https://stackoverflow.com/a/68778908
            ndk {
                debugSymbolLevel "FULL"
            }
        }
    }

    buildFeatures {
        viewBinding true
        mlModelBinding true
    }

    androidResources {
        noCompress = 'tflite'
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    implementation 'com.google.android.gms:play-services-games-v2:19.0.0'
    // SDK 21-25 should be fixed in 2.15, see https://github.com/tensorflow/tensorflow/issues/61951
    // Releases: https://central.sonatype.com/artifact/org.tensorflow/tensorflow-lite/versions
    implementation 'org.tensorflow:tensorflow-lite:2.12.0'

    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}